import React, { useRef } from 'react'
import { NotFoundComponent } from '../Components/NotFoundComponent.tsx/NotFoundComponent'
import { useNavigate } from 'react-router-dom'

export const NotFound = () => {

  return (
    
    
   <NotFoundComponent />
  )
}
